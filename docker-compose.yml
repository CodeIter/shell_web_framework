version: "3.9"

services:
  app:
    build: .
    container_name: bash-web-framework
    restart: unless-stopped
    ports:
      - "8000:8000"
    env_file:
      - .env
    volumes:
      - sessions:/app/sessions
      - data:/app/data
      - tmp:/app/tmp

volumes:
  sessions:
  data:
  tmp:

